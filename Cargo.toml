[package]
name = "ai-ark"
version = "0.1.0"
edition = "2021"

[dependencies]
wasi-http-client = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = "z"
lto = true
strip = true